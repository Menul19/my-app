name: menul_music_pro
description: "Premium Music Player with Bass Visualizer"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  on_audio_query: ^2.9.0
  just_audio: ^0.9.36
  permission_handler: ^11.3.0
  google_fonts: ^6.1.0

flutter:
  uses-material-design: true
